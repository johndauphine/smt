source:
  type: mssql
  host: localhost
  port: 1433
  database: CRM_MSSQL
  user: sa
  password: TestPass2024
  schema: dbo
  trust_server_cert: true

target:
  type: postgres
  host: localhost
  port: 5432
  database: postgres
  user: postgres
  password: TestPass2024
  schema: crm_accept_mssql_to_postgres
  ssl_mode: disable

schema_generation:
  mode: deterministic
  unknown_type_policy: fail

ai_review:
  enabled: false

migration:
  create_indexes: true
  create_foreign_keys: true
  create_check_constraints: true
  data_dir: .acceptance-artifacts/crm/mssql-to-postgres
