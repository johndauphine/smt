source:
  type: postgres
  host: localhost
  port: 5432
  database: crm_pg
  user: postgres
  password: TestPass2024
  schema: public
  ssl_mode: disable

target:
  type: mysql
  host: localhost
  port: 3306
  database: mysql
  user: root
  password: TestPass2024
  schema: crm_accept_postgres_to_mysql

schema_generation:
  mode: deterministic
  unknown_type_policy: fail

ai_review:
  enabled: false

migration:
  create_indexes: true
  create_foreign_keys: true
  create_check_constraints: true
  data_dir: .acceptance-artifacts/crm/postgres-to-mysql
